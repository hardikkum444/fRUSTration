//logical and bitwise ops

fn main(){
    
    assert!(true || false);
    //assert_eq!(true, false); this will panic and will gen a warning
    assert!(!true == false);
    println!("Success");
    
    //XOR op --> if same (1,1) then 0 and if diff(0,1) then 1
}